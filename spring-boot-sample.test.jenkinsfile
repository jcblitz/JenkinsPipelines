pipeline {
    agent any
      parameters {
        string(name: 'buildVersion', description: 'Which version to deploy?')
    }
    stages {
        stage('Retrieving Build') {
            steps {
                echo "Getting release ${params.buildVersion}"
            }
        }
    }
}